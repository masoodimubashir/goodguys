
const MenuTitle = ({children}) => {
    return (
        <>
            <li className="menu-title">
                <span>{children}</span>
            </li>
        </>
    )
}

export default MenuTitle